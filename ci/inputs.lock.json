{
  "version": 1,
  "description": "Pinned inputs for workflow_dispatch replay mode. Replace the placeholder values below with fields from a build-inputs artifact before enabling use_input_lock=true. The akmods upstream ref is intentionally not stored here; it comes from ci/defaults.json so there is one source of truth.",
  "base_image": "REPLACE_ME",
  "build_container": "REPLACE_ME",
  "zfs_minor_version": "2.4"
}
